/// Generated model class for User
/// DO NOT EDIT - This file is automatically generated

import 'dart:async';
import 'package:pg_dorm/config/config.dart';
import '../database/postgres/query/query.dart';
import '../database/postgres/query/operator.dart';
import '../queries/user_query.dart';
import 'base_model.dart';
import 'database_context.dart';

class User extends BaseModel {
  // Properties
  int id;
  String first_name;
  String? last_name;
  String email;
  String password;
  DateTime date_joined;

  // Constructor
  User({
    required this.id,
    required this.first_name,
    this.last_name,
    required this.email,
    required this.password,
    required this.date_joined,
  });

  // Table information
  static const String tableName = 'user';

  // Serialization
  @override
  Map<String, dynamic> toMap() {
    return {
      'id': id,
      'first_name': first_name,
      'last_name': last_name,
      'email': email,
      'password': password,
      'date_joined': date_joined,
    };
  }

  // Deserialization
  factory User.fromMap(Map<String, dynamic> map) {
    return User(
      id: map['id'] as int,
      first_name: map['first_name'] as String,
      last_name: map['last_name'] as String?,
      email: map['email'] as String,
      password: map['password'] as String,
      date_joined: map['date_joined'] != null ? DateTime.parse(map['date_joined']) : null,
    );
  }

  // Validation
  @override
  List<String> validate() {
    final errors = <String>[];

    if (id == null) {
      errors.add('id is required');
    }
    if (first_name == null) {
      errors.add('first_name is required');
    }
    if (email == null) {
      errors.add('email is required');
    }
    if (email != null && email!.trim().isEmpty) {
      errors.add('email cannot be blank');
    }
    if (password == null) {
      errors.add('password is required');
    }
    if (password != null && password!.trim().isEmpty) {
      errors.add('password cannot be blank');
    }
    if (date_joined == null) {
      errors.add('date_joined is required');
    }

    return errors;
  }

  // CRUD Operations
  static Future<User> create({
    required String first_name,
    String? last_name,
    required String email,
    required String password,
    required DateTime date_joined,
    String? connectionName,
  }) async {
    final instance = User(
      first_name: first_name,
      last_name: last_name,
      email: email,
      password: password,
      date_joined: date_joined,
    );

    // Validate
    final validationErrors = instance.validate();
    if (validationErrors.isNotEmpty) {
      throw ValidationException(validationErrors);
    }

    // Execute insert
    final context = DatabaseContext.instance;
    final result = await context.insert(
      tableName: tableName,
      data: instance.toMap(),
      connectionName: connectionName,
    );

    instance.id = result['id'] as int;
    return instance;
  }

  static Future<User?> findById(
    int id, {
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final results = await context.query(
      sql: 'SELECT * FROM $tableName WHERE id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    if (results.isEmpty) return null;
    return User.fromMap(results.first);
  }

  static Future<List<User>> findAll({
    String? where,
    List<dynamic>? parameters,
    String? orderBy,
    int? limit,
    int? offset,
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final buffer = StringBuffer('SELECT * FROM $tableName');

    if (where != null) {
      buffer.write(' WHERE $where');
    }

    if (orderBy != null) {
      buffer.write(' ORDER BY $orderBy');
    }

    if (limit != null) {
      buffer.write(' LIMIT $limit');
    }

    if (offset != null) {
      buffer.write(' OFFSET $offset');
    }

    final results = await context.query(
      sql: buffer.toString(),
      parameters: parameters ?? [],
      connectionName: connectionName,
    );

    return results.map((row) => User.fromMap(row)).toList();
  }

  Future<bool> save({String? connectionName}) async {
    // Validate
    final validationErrors = validate();
    if (validationErrors.isNotEmpty) {
      throw ValidationException(validationErrors);
    }

    final context = DatabaseContext.instance;
    final affectedRows = await context.update(
      tableName: tableName,
      data: toMap(),
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  Future<bool> delete({String? connectionName}) async {
    final context = DatabaseContext.instance;
    final affectedRows = await context.delete(
      tableName: tableName,
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  static Future<bool> deleteById(
    int id, {
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final affectedRows = await context.delete(
      tableName: tableName,
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  /// Create a query builder for this model
  static UserQuery query() => UserQuery();

  /// Find records using query builder
  static Future<List<User>> where(
    Operation operation, {
    String? connectionName,
  }) async {
    final queryBuilder = UserQuery().where(operation);
    final context = DatabaseContext.instance;
    final sql = queryBuilder.toString();
    
    final results = await context.query(
      sql: sql,
      parameters: [],
      connectionName: connectionName,
    );

    return results.map((row) => User.fromMap(row)).toList();
  }

  /// Find first record matching query
  static Future<User?> firstWhere(
    Operation operation, {
    String? connectionName,
  }) async {
    final queryBuilder = UserQuery().where(operation).limit(1);
    final context = DatabaseContext.instance;
    final sql = queryBuilder.toString();
    
    final results = await context.query(
      sql: sql,
      parameters: [],
      connectionName: connectionName,
    );

    return results.isNotEmpty ? User.fromMap(results.first) : null;
  }

  @override
  String toString() {
    return 'User{id: $id}';
  }

}

class ValidationException implements Exception {
  final List<String> errors;
  ValidationException(this.errors);

  @override
  String toString() => 'ValidationException: ${errors.join(", ")}';
}
