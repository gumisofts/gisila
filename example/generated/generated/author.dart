part of '../models.dart';

/// Generated model class for Author
/// DO NOT EDIT - This file is automatically generated

class Author extends BaseModel {
  // Properties
  int id;
  String first_name;
  String? last_name;
  String email;

  // Constructor
  Author({
    required this.id,
    required this.first_name,
    this.last_name,
    required this.email,
  });

  // Table information
  static const String tableName = 'author';

  // Serialization
  @override
  Map<String, dynamic> toMap() {
    return {
      'id': id,
      'first_name': first_name,
      'last_name': last_name,
      'email': email,
    };
  }

  // Deserialization
  factory Author.fromMap(Map<String, dynamic> map) {
    return Author(
      id: map['id'] as int,
      first_name: map['first_name'] as String,
      last_name: map['last_name'] as String?,
      email: map['email'] as String,
    );
  }

  // Validation
  @override
  List<String> validate() {
    final errors = <String>[];

    if (id == null) {
      errors.add('id is required');
    }
    if (first_name == null) {
      errors.add('first_name is required');
    }
    if (email == null) {
      errors.add('email is required');
    }
    if (email != null && email!.trim().isEmpty) {
      errors.add('email cannot be blank');
    }

    return errors;
  }

  // CRUD Operations
  static Future<Author> create({
    required String first_name,
    String? last_name,
    required String email,
    String? connectionName,
  }) async {
    final instance = Author(
      first_name: first_name,
      last_name: last_name,
      email: email,
    );

    // Validate
    final validationErrors = instance.validate();
    if (validationErrors.isNotEmpty) {
      throw ValidationException(validationErrors);
    }

    // Execute insert
    final context = DatabaseContext.instance;
    final result = await context.insert(
      tableName: tableName,
      data: instance.toMap(),
      connectionName: connectionName,
    );

    instance.id = result['id'] as int;
    return instance;
  }

  static Future<Author?> findById(
    int id, {
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final results = await context.query(
      sql: 'SELECT * FROM $tableName WHERE id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    if (results.isEmpty) return null;
    return Author.fromMap(results.first);
  }

  static Future<List<Author>> findAll({
    String? where,
    List<dynamic>? parameters,
    String? orderBy,
    int? limit,
    int? offset,
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final buffer = StringBuffer('SELECT * FROM $tableName');

    if (where != null) {
      buffer.write(' WHERE $where');
    }

    if (orderBy != null) {
      buffer.write(' ORDER BY $orderBy');
    }

    if (limit != null) {
      buffer.write(' LIMIT $limit');
    }

    if (offset != null) {
      buffer.write(' OFFSET $offset');
    }

    final results = await context.query(
      sql: buffer.toString(),
      parameters: parameters ?? [],
      connectionName: connectionName,
    );

    return results.map((row) => Author.fromMap(row)).toList();
  }

  Future<bool> save({String? connectionName}) async {
    // Validate
    final validationErrors = validate();
    if (validationErrors.isNotEmpty) {
      throw ValidationException(validationErrors);
    }

    final context = DatabaseContext.instance;
    final affectedRows = await context.update(
      tableName: tableName,
      data: toMap(),
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  Future<bool> delete({String? connectionName}) async {
    final context = DatabaseContext.instance;
    final affectedRows = await context.delete(
      tableName: tableName,
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  static Future<bool> deleteById(
    int id, {
    String? connectionName,
  }) async {
    final context = DatabaseContext.instance;
    final affectedRows = await context.delete(
      tableName: tableName,
      where: 'id = ?',
      parameters: [id],
      connectionName: connectionName,
    );

    return affectedRows > 0;
  }

  /// Create a query builder for this model
  static AuthorQuery query() => AuthorQuery();

  /// Find records using query builder
  static Future<List<Author>> where(
    Operation operation, {
    String? connectionName,
  }) async {
    final queryBuilder = AuthorQuery().where(operation);
    final context = DatabaseContext.instance;
    final sql = queryBuilder.toString();
    
    final results = await context.query(
      sql: sql,
      parameters: [],
      connectionName: connectionName,
    );

    return results.map((row) => Author.fromMap(row)).toList();
  }

  /// Find first record matching query
  static Future<Author?> firstWhere(
    Operation operation, {
    String? connectionName,
  }) async {
    final queryBuilder = AuthorQuery().where(operation).limit(1);
    final context = DatabaseContext.instance;
    final sql = queryBuilder.toString();
    
    final results = await context.query(
      sql: sql,
      parameters: [],
      connectionName: connectionName,
    );

    return results.isNotEmpty ? Author.fromMap(results.first) : null;
  }

  @override
  String toString() {
    return 'Author{id: $id}';
  }
}
